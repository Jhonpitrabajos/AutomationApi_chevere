# Base URL for reqres.in API
base.url=https://reqres.in

# API Key for reqres.in - REMOVED (not needed for public API)
# api.key=reqres-free-v1

# Endpoints
endpoint.users=/api/users
endpoint.register=/api/register
endpoint.login=/api/login

# File paths
file.users=src/test/resources/data/users.json

# Additional configurations
http.timeout=5000
